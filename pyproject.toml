[project]
name = "textpulse-forecast-lab"
version = "2.0.0"
description = "NLP topic monitoring and time-series forecasting for dated text."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
