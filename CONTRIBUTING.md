# Contributing

1. Create a branch from `main`.
2. Install development dependencies with `pip install -r requirements-dev.txt`.
3. Add or update tests for every behaviour change.
4. Run `pytest` and the critical flake8 check before opening a pull request.
5. Keep user-facing errors clear and avoid storing uploaded data on disk.
